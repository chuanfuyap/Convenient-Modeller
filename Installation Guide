INSTALLATION GUIDE:

OPTION 1:

Source code can be viewed/compiled with Java IDE of choice (if interested) and the Java libraries can be found accordingly.

	Guava Google version18 https://github.com/google/guava/wiki/Release18

	Apache Common Maths version 3.5 http://archive.apache.org/dist/commons/math/

	Java Native Access (JNA) https://github.com/java-native-access/jna

	Java Systems Biology Markup Language (JSBML) version 1 http://sbml.org/Software/JSBML

OPTION 2:

Feel free to download on the GRaPe2.jar found. This is the compiled version that has all the Java libraries


C LIBRARIES:

Please download the following C Libraries, compile and install them in your system.

	SOSLIB version 1.7 https://github.com/raim/SBML_odeSolver

	libSBML version 5.X.X https://sourceforge.net/projects/sbml/files/libsbml/5.13.0/

	SUNDIALS version 2.3 http://computation.llnl.gov/projects/sundials/sundials-software

NOTES FOR COMPILATION IN LINUX:
	
SUNDIALS

./configure --enable-shared CFLAGS=-fPIC
make
make install

LIBSBML
download the uncompiled version from sourceforge libSBML-5.13.0-core-src.tar.gz <----- source code (only), gzip'ed tar archive

./configure
make
make install

SOSlib

./configure --with-libsbml=LIBSBMLPATH --with-sundials=SUNDIALSPATH
make
make install

the paths should be /usr/local/lib

CFLAGS=-fPIC if needed


FOLLOWING COMPILATION AND INSTALLAION AND THERE IS ERROR:
User can try the following: 

	write this into either .bash or .tcsh check by typing "ps" into terminal
	export LD_LIBRARY_PATH="/usr/local/lib" #fix for soslib 



AFTER DOWNLOADING .jar FILE AND SUCCESSFULLY INSTALLING C LIBRARIES:

make an empty folder next to where GRaPe2.jar is found and name it "lib", download the files found in https://github.com/chuanfuyap/soslibJNIC
and put the files into the newly created "lib" folder.

user can now run GRaPe2.jar by typing:
java -jar GRaPe2.jar
